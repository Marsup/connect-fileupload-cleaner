{
  "name": "connect-fileupload-cleaner",
  "version": "0.0.1",
  "description": "Automatically removes temporary files created by connect's multipart middleware once the request is answered.",
  "main": "lib/index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --reporter spec"
  },
  "repository": "git://github.com/Marsup/connect-fileupload-cleaner",
  "license": "BSD",
  "devDependencies": {
    "connect": "~2.4.4",
    "mocha": "~1.4.2",
    "should": "~1.1.0",
    "request": "~2.11.0",
    "glob": "~3.1.12"
  }
}
